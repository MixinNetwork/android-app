package one.mixin.android.ui.wallet.transfer.data

enum class TransferType {
    transfer,
    addressTransfer,
    withdraw,
    mutlSign,
    unMulSign,
    addAddress,
    deleteAddress,
    nft
}
