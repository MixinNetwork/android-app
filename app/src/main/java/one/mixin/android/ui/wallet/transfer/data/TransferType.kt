package one.mixin.android.ui.wallet.transfer.data

enum class TransferType {
    transfer,
    addressTransfer,
    withdraw,
    mutlSign,
    unMulSign,
    approve,
    reject,
    addAddress,
    deleteAddress,
    nft,
    nftRelease,
    signed,
}
