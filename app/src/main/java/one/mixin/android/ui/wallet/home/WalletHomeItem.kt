package one.mixin.android.ui.wallet.home

enum class WalletHomeType {
    PRIVACY,
    CLASSIC,
}

enum class WalletHomeDataState {
    EMPTY,
    CACHE,
    DATABASE,
}

enum class WalletHomeCardType {
    EMPTY_GUIDE,
    BALANCE,
    BANNER,
    POSITIONS,
    TOP_MOVERS,
    TOKENS,
    TRANSACTIONS,
    REFERRAL,
    SUPPORT,
}
