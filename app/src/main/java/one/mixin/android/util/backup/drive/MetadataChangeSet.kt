package one.mixin.android.util.backup.drive

class MetadataChangeSet {

    companion object {
        const val INDEXABLE_TEXT_SIZE_LIMIT_BYTES = 131072
        const val CUSTOM_PROPERTY_SIZE_LIMIT_BYTES = 124
        const val MAX_PUBLIC_PROPERTIES_PER_RESOURCE = 30
        const val MAX_PRIVATE_PROPERTIES_PER_RESOURCE_PER_APP = 30
        const val MAX_TOTAL_PROPERTIES_PER_RESOURCE = 100
    }
}